import React from "react";
// import { Checkbox, Dialog, makeStyles } from "@material-ui/core";
// import FiberManualRecordOutlinedIcon from '@material-ui/icons/FiberManualRecordOutlined';
// import FiberManualRecordSharpIcon from '@material-ui/icons/FiberManualRecordSharp';
// import Edit from "#UI/icons/Edit";


const BtnActive = () => {


    return(
        <div>
            {/* <div className={classes.edit}>
                <Edit></Edit>
                <button type="submit" className={`${classes.btn__edit}`} onClick={() => props.Router.push(`/editPage/${offerID}`)}>
                Редактировать
                </button>
            </div>
            <button
                value={offer.id}
                onClick={(e) => props.pushCheck(e)}
                className={classes.btn__unpublish}>
                Снять с публикации
            </button> */}
    </div>	
    )
}
export default BtnActive